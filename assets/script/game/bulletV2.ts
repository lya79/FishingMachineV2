import { ESkill, Skill, Tower } from "../common/setting";

// TODO 改用 tween去移動子彈減少計算
export class BulletV2 extends cc.Component {
    public init(
        roomLevel: number,
        tower: Tower,
        location: cc.Vec2,
        rotataion: number,
    ) {
    }
}